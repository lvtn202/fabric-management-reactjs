
const styles = (theme) => ({
  divider: {
    marginTop: theme.spacing(2),
  },
  grid: {
    flexGrow: 1,
    marginBottom: theme.spacing(4),
    marginTop: theme.spacing(4),
  },
  tableContainer: {
    marginTop: theme.spacing(2),
  },
  select: {
    display: 'flex',
    flexWrap: 'wrap',
  },
});

export default styles;