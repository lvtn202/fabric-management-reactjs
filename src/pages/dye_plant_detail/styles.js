const styles = (theme) => ({
  divider: {
    marginTop: theme.spacing(2),
  },
  grid: {
    marginTop: theme.spacing(2),
  },
  tableContainer: {
    marginTop: theme.spacing(2),
  },
  select: {
    display: "flex",
    flexWrap: "wrap",
  },
});

export default styles;
