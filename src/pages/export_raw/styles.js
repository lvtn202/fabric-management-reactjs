const styles = (theme) => ({
  root: {
    width: "100%",
    margin: theme.spacing(2),
  },
  selectField: {
    width: "80%",
  },  
  table: {
    minWidth: 650,
  },
  tableContainer: {
    margin: theme.spacing(2),
    maxHeight: 440,
    maxWidth: "60%"
  },
});

export default styles;
