const styles = (theme) => ({
  tabs: {
    marginTop: theme.spacing(2),
    flexGrow: 1,
  },
  divider: {
    marginTop: theme.spacing(2),
  },
  table: {
    minWidth: 650,
  },
  tableContainer: {
    marginTop: theme.spacing(2),
    maxHeight: 500,
  },
});

export default styles;
