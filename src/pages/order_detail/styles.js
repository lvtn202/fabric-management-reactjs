const styles = (theme) => ({
  divider: {
    marginTop: theme.spacing(2),
  },
  tableContainer: {
    marginTop: theme.spacing(2),
  },
  grid: {
    marginTop: theme.spacing(2),
  },
});

export default styles;
